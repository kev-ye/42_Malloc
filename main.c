#include "malloc.h"
// #include <stdlib.h>

int main()
{
    // __unused
    // char *s = malloc(4000);
    // free(s);

    // show_alloc_mem_info();

    // __unused
    // char *s2 = malloc(2000);
    // free(s2);

    // show_alloc_mem_info();

    // show_alloc_mem_leak();

    // show_alloc_mem();
    return (0);
}